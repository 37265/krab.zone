# How to start the krab.zone container
1. Make sure you have Docker installed.
2. In the main repository directory, run `docker compose up` and the container will start automatically

You might run into a problem if your host is already using port 80 for something. Tough luck!
